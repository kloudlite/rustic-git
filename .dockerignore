# The Dockerfile copies Cargo.*, src/, crates/, bins/ and tests/ (every workspace member manifest,
# needed for cargo-chef and `cargo build`) — everything else is context upload for nothing.
target
.git
web
.local
.claude
.worktrees
docs
deploy
