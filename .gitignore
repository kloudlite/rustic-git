/target
# Local-run scratch: per-node caches and generated SSH host keys.
/.local
# Older runs wrote these at the root; keep ignoring strays.
/cache*
host_key*
/.superpowers
/.claude/worktrees

# web
web/node_modules
web/**/node_modules
web/**/.next
web/.turbo
/.turbo
/.worktrees/
