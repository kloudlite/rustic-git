# Over-engineering audit — 2026-09-03 (master 4c7e94c9)

Scope: over-engineering and complexity only. Correctness/security/performance excluded by request.

native: the whole Cosmos DB tier — `azure_data_cosmos` + `azure_core` + `azure_core_macros` (and the duplicate `reqwest 0.12` they drag in next to the graph's `reqwest 0.13`) exist for an 85-line file storing a handful of `Region` rows. Kubernetes is already the reconcile substrate and every other object is a CRD: make `Region` a cluster-scoped CRD (or one ConfigMap) written by `/v1/regions`, delete `cosmos.rs`, `store.rs`, `MetaStore`, `MemStore`, `from_env`, `tests/meta_store.rs`, and the `COSMOS_ENDPOINT`/`COSMOS_KEY`/`COSMOS_DB` triple. [crates/workspaces/src/cosmos.rs:1, crates/workspaces/src/store.rs:17, Cargo.toml (azure_data_cosmos)]
delete: `VolumeSource::RestoreOf` and everything that keeps it parseable — the variant, its four match arms, `RESTORE_OF_GONE`, the `RestoreMechanismGone` reason and the validation arm. `/v1` has never written it since Task 8 and the cluster (created 2026-08-27, every object recreated since 2026-09-03) holds no pre-cutover object, so the "kept so old specs deserialize" tolerance is protecting nothing. [crates/workspaces/src/crd.rs:94, bins/agent/src/controller/volume.rs:266, crates/workspaces/src/engine/ops.rs:18]
shrink: four hand-built compare-and-set JSON patches with identical `Test`+`Replace`+`422|409 => Ok(false)` bodies. One `async fn cas(api, name, path, from, to) -> Result<bool, kube::Error>`; `take_volume`/`release_volume` become one line each, `detach_volume`/`attach_volume` keep only their list-diff prologue. Also folds `peer.rs`'s fifth copy. [bins/agent/src/controller/volume.rs:412-540, bins/agent/src/peer.rs:1008]
yagni: `MetaStore` trait + `StoreErr` (one-variant enum) + `MemStore` — two methods, `put_region`/`regions`, one real impl and one dev fallback. Falls out with the Cosmos cut; if Cosmos stays, drop the trait and let `bins/api` hold `Arc<CosmosStore>` directly. [crates/workspaces/src/store.rs:11-45]
yagni: `crates/api/src/lib.rs`'s three re-export shim modules (`auth`, `proxy`, and the `pub(crate) use` block) exist only "to keep every call site unchanged" after the code moved to `core`/`storage`. Rename the call sites once; the shim is a permanent indirection paying for a one-time sed. [crates/api/src/lib.rs:15-30]
delete: `examples/views_bench.rs` and `examples/tree_bench.rs` — referenced by no CI job, script, doc or CLAUDE.md; they compile on every `cargo test`. [examples/views_bench.rs:1, examples/tree_bench.rs:1]
delete: `Workspace.live_state` — documented as unused, always serialized as `null`, kept only because the web type names it. Delete the field, the `Value::Null` that fills it, and the TS declaration. [crates/workspaces/src/model.rs:84, crates/workspaces/src/api.rs:409, web/apps/web/src/lib/api.ts:709]
stdlib: `hex_prefix`'s `.map(|b| format!("{b:02x}")).collect()` — the `hex` crate is already a workspace dependency. `hex::encode(&sha2::Sha256::digest(raw)[..bytes])`. [crates/workspaces/src/crd.rs:852]
yagni: `Directory`'s four default method bodies — every default is the "unwired" answer and the one production impl overrides all of them; only test stubs use the defaults, and a partial stub silently reads as a live-but-empty directory. Make the methods required and let each stub say what it means. [crates/workspaces/src/api.rs:60-80]
stdlib: `rfc3339(ms)` reaches through `mongodb::bson::DateTime` to format an instant in a module that has nothing else to do with mongo; `chrono` is already a workspace dep — `chrono::DateTime::from_timestamp_millis(ms).map(|d| d.to_rfc3339()).unwrap_or_default()`. [crates/api/src/credentials.rs:677]
yagni: `bins/gateway/src/lib.rs` is four lines of `pub mod` + a re-export of `tunnel::{app, Gateway}`; the two callers can name `tunnel::` directly. Same for `src/lib.rs`, a one-line doc comment that exists only to give `tests/` a host package. [bins/gateway/src/lib.rs:1, src/lib.rs:1]
yagni: tunables with a default, one reader and no setter anywhere in `deploy/` — `RUSTIC_GIT_MAX_LAYER`, `RUSTIC_GIT_ALLOW_MEM_FLEET`, `WS_BASE_PACKAGES`, `RUSTIC_GIT_LOG_FORMAT`. Inline the constant; add the env read back the day someone needs to change it in a running cluster. [crates/registry/src/blobs.rs:30, crates/storage/src/config.rs:132]
native: `react-markdown` + `remark-gfm` serve exactly one component (README rendering) while `shiki` is already installed for highlighting. Worth checking whether shiki's markdown path covers it before keeping two more npm trees. [web/apps/web/src/components/repo/code.tsx:3]

net: -900 lines, -4 deps possible (azure_data_cosmos, azure_core, azure_core_macros, the duplicate reqwest 0.12; -2 more npm if react-markdown/remark-gfm go).
